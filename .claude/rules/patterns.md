# Patterns Index

- **Result type**: `Result<T,E>` discriminated union with `ok()`/`err()` constructors — all fallible core functions return this, never throw → [patterns/result-type.md]
- **Error hierarchy**: `UserError`, `GitError`, `ScanError`, `NetworkError` extend `SkilltapError` with optional `hint` field → [patterns/error-hierarchy.md]
- **Zod boundary**: Zod schema is source of truth for type + validation; `safeParse` + `z.prettifyError` at every data ingestion point; use `.prefault({})` not `.default({})` for nested objects; `parseWithResult(schema, raw, label)` helper at every boundary → [patterns/zod-boundary.md]
- **Callback-driven options**: Core functions accept typed option objects with async callbacks (`onWarnings`, `onSelectSkills`, `onConfirmInstall`, etc.) for UI decision points; omit = auto-proceed → [patterns/callback-driven-options.md]
- **Policy composition**: `composePolicy(config, flags)` → `EffectivePolicy` (pure); `composePolicyForSource` adds trust overlay; `loadPolicyOrExit()` is CLI-layer wrapper; file: `core/src/policy/compose.ts` → [patterns/policy-composition.md]
- **Output interface**: All command output through `setupOutput(args)` → `Output` handle; 3 modes: `tty` (clack/ANSI), `plain` (text), `json` (structured events); replaces old agent-mode branching → [patterns/output-interface.md]
- **SourceAdapter strategy**: Plain object literals implementing `{ name, canHandle(), resolve() }`; resolver iterates `ADAPTERS[]` in priority order → [patterns/source-adapter.md]
- **AgentAdapter strategy**: `AgentAdapter` interface with `detect()`/`invoke()`; factory functions (`createCliAdapter`, `createCustomAdapter`, `createOllamaAdapter`); priority resolution via `resolveAgent()` → [patterns/agent-adapter-strategy.md]
- **Bun shell git**: All git via `wrapGit<T>(fn, msg, hint?)` wrapper + `extractStderr(e)` helper; returns `Result<T, GitError>`; `.quiet()` on all commands → [patterns/bun-shell-git.md]
- **Config I/O**: Load = ensureDirs → exists check → read → parse format → safeParse → ok; Save = ensureDirs → serialize → Bun.write → ok; state.json uses `loadState`/`saveState` → [patterns/config-io.md]
- **Security detector composition**: 7 independent `(content: string) => PatternMatch[]` functions composed in a for-loop in `scanStatic()`; add new detectors by appending to the `detectors` array → [patterns/security-detector-composition.md]
- **InstallResult with warnings**: `installSkill()` returns `{ records, warnings, semanticWarnings, updates, pluginRecord? }`; unified `onWarnings(warnings, kind, name)` callback; `skipScan: true` for tests → [patterns/install-result-with-warnings.md]
- **Apply state change**: `applySkillStateChange({scope, projectRoot, mutate, manifestSync?})` — atomic load→mutate→save for skills[]; diffs before/after to fire manifest sync hooks → [patterns/apply-state-change.md]
- **Fixture repo factory**: `createX()` returns `{ path, cleanup }` — copy static fixture dir, `initRepo`, `commitAll`; always `dot:true` in Bun.Glob.scan → [patterns/test-fixtures.md]
- **Result test assertions**: `expect(result.ok).toBe(true)` then `if (!result.ok) return` guard; `VALID_*` constants with spread for schema variants → [patterns/test-result-assertions.md]
- **CLI subprocess testing**: Use `runSkilltap(args, homeDir, configDir)` + `createTestEnv()` from `@skilltap/test-utils`; `cliCmd()` routes to compiled binary when `SKILLTAP_TEST_BIN` set → [patterns/cli-subprocess-testing.md]
- **Test env isolation**: `createTestEnv()` from `@skilltap/test-utils` returns `{ homeDir, configDir, cleanup() }`; sets `SKILLTAP_HOME`/`XDG_CONFIG_HOME`; replaces manual `beforeEach`/`afterEach` env setup → [patterns/test-env-isolation.md]
- **Injectable dependencies**: Core functions with external I/O accept `_dep = realImpl` optional params; tests pass mocks as 2nd/3rd args; private `type Fn = typeof realFn` aliases enforce signature → [patterns/injectable-dependencies.md]
- **Graceful fallback chain**: Optional verifiers return `T | null` (never `Result`); caller cascades through priority tiers; outer `try/catch` guarantees a valid result is always returned → [patterns/graceful-fallback-chain.md]
- **Adapter-driven branching**: After `resolveSource()`, `resolved.adapter` (and `record.repo?.startsWith("npm:")` on update) gates distinct install/update/trust paths per source type → [patterns/adapter-driven-branching.md]
- **Single-source definitions**: One authoritative constant per enumerable concept; all consumers import/derive — never copy literal values. Agent metadata (`AGENT_PATHS`, `AGENT_LABELS`, `VALID_AGENT_IDS`) lives in `core/src/symlink.ts`; config enum arrays (`SCAN_MODES`, etc.) are exported constants in `core/src/schemas/config.ts` → [patterns/single-source-definitions.md]
- **scopeBase**: `scopeBase(scope, projectRoot?)` pure helper in `paths.ts` — single-source scope-to-base-dir resolution; all scope-conditional dir construction goes through this or its derived helpers (`skillInstallDir`, etc.) → [patterns/scope-base.md]
- **json-state I/O**: `loadJsonState<T>(path, schema, label, default)` / `saveJsonState(path, data, label, projectRoot?, ensureDirs)` generic helpers in `json-state.ts`; `loadState`/`saveState` delegate to these — no ad-hoc JSON file I/O in state modules → [patterns/json-state-io.md]
