---
name: skilltap
description: Use skilltap to inspect and manage the local Codex and Claude Code environment.
---

# skilltap

Use the `skilltap` binary to inspect and manage the local agent environment.
This package provides the discovery surface; consult `skilltap --help` and the
relevant leaf command for exact syntax before taking an action.
