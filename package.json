{
  "name": "skilltap-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --bun packages/cli/src/index.ts",
    "test": "bun test --recursive packages/",
    "build": "bun build packages/cli/src/index.ts --compile --outfile skilltap"
  },
  "devDependencies": {
    "bun-types": "^1.3.10"
  }
}
